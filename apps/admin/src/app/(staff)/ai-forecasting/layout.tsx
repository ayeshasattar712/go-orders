import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'AI Demand Forecasting' };

export default function AiForecastingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
